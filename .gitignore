node_modules/
dist/

# Web UI build output — rebuilt by `npm start` / start.bat locally and by CI
# (deploy.yml) for GitHub Pages. Committing it caused stale-base-path blank pages.
docs/

.env
.env.*
!.env.example
.DS_Store
npm-debug.log*

# Runtime snapshot data (generated by server at runtime)
data/snapshots/*.json
data/snapshots/*.txt

# AI tooling artifacts — not part of the project
graphify-out/
ruvector.db
memory.db
.agents/
.opencode/
.superpowers/
data/skills/
